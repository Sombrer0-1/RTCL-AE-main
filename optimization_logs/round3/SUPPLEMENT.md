# 正式矩阵后的补充验证（2026-09-24，补充结果前记录）

主矩阵 16 次运行已完成。逐次负载记录显示：突发组 seed 101 的末次 Slack 运行负载从 9.8 升至 25.32，seed 202 四次运行从 25.32 升至约 55。不能把这些运行的策略差异当作已隔离环境干扰的效果，更不能用跨种子均值掩盖干扰。

因此追加且仅追加：

1. 一条 ResNet20 / 2 epochs / seed 101 / FIFO 诊断运行，用独立包装器计时 `before_training_epoch` 到第一次 `before_training_iteration` 回调的区段；不改变控制律，不与主矩阵墙钟结果合并。
2. 四条 ResNet56 / 1 epoch / 突发请求 / seed 303 的新种子对照。请求数、SLO、窗口、模型、训练参数、所有控制器参数保持主矩阵设置。顺序固定 Slack、FIFO、Neural、Debt。留档所有结果，不按优胜者挑选/追加种子。主机仍非独占，报告负载范围。

这不是原设计内的额外训练或调参，也不能消除共享主机的全部混杂；它用于检验原突发组结果能否在新的同设置运行中重现。分析时单独展示，不合并为一组“平均加速”。
